/**
 * 布局组件模块
 *
 * 这个模块包含应用的布局组件，用于构建页面的整体结构。
 *
 * 设计原则：
 * - 响应式：适配不同屏幕尺寸
 * - 可配置：支持自定义侧边栏、头部等
 * - 一致性：统一的布局风格
 *
 * 包含的组件：
 *
 * 1. DashboardLayout（仪表盘布局）
 *    - 用途：管理后台、商户中心的主布局
 *    - 结构：顶部导航 + 侧边栏 + 主内容区
 *    - 特性：侧边栏可折叠、响应式适配
 *
 * 布局结构示意：
 * ```
 * ┌─────────────────────────────────────┐
 * │           顶部导航栏                 │
 * ├──────────┬──────────────────────────┤
 * │          │                          │
 * │  侧边栏   │       主内容区           │
 * │          │                          │
 * │          │                          │
 * └──────────┴──────────────────────────┘
 * ```
 *
 * 使用示例：
 * ```vue
 * <template>
 *   <DashboardLayout>
 *     <template #sidebar>
 *       <!-- 侧边栏内容 -->
 *     </template>
 *     <template #default>
 *       <!-- 主内容 -->
 *     </template>
 *   </DashboardLayout>
 * </template>
 * ```
 */
export { default as DashboardLayout } from './DashboardLayout.vue'
